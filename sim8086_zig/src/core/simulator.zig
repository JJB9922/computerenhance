pub const registers = struct {
    al: u8,
    cl: u8,
    dl: u8,
    bl: u8,
    ah: u8,
    ch: u8,
    dh: u8,
    bh: u8,

    ax: u8,
    cx: u8,
    dx: u8,
    bx: u8,
    sp: u8,
    bp: u8,
    si: u8,
    di: u8,
};
